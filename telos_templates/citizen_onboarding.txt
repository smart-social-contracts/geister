Find a realm you like
Join the realm
Set your avatar
Use get_my_invoices to list your pending invoices (automatically filtered to your identity)
For each pending invoice:
  a. Get the deposit address: call member_dashboard extension get_invoice_deposit_address with invoice_id
  b. Send ckBTC to the vault canister at the invoice's subaccount
  c. Verify payment: call vault extension refresh_invoice with invoice_id
Vote on proposals (call voting extension cast_vote with proposal_id and vote choice)
Create a proposal (call voting extension submit_proposal with title, description, and code_url)
